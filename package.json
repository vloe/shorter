{
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"format": "prettier --write .",
		"web": "pnpm --filter @sh/web -- ",
		"core": "pnpm --filter @sh/server -- ",
		"config": "pnpm --filter @sh/config -- "
	},
	"devDependencies": {
		"prettier": "^3.2.5",
		"prettier-plugin-svelte": "^3.2.2",
		"prettier-plugin-tailwindcss": "^0.5.12",
		"turbo": "^1.13.0",
		"wrangler": "^3.39.0"
	}
}
